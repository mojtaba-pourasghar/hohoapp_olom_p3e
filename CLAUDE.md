# هوهو علوم — یادداشت‌های پروژه

اپ اندروید (جاوا) آموزش علوم تجربی سوم دبستان، بر اساس کتاب درسی ۱۴۰۵
(`project/uploads/sevom-olom-tajrobi-1405.pdf`). ساختارش از «هوهو ریاضی»
(`mojtaba-pourasghar/hohoapp_math_p3e`) آمده؛ اسکلت صفحه‌ها، هوهو، صدا، پنل والدین و
ذخیره‌ی پیشرفت همان است. فرقِ اصلی: **هوهو مثل معلم درس می‌دهد و تصویرهای کتاب را جدا و متحرّک نشان می‌دهد.**

## قاعده‌ی همیشگی: بعد از هر تغییر

۱. **داده‌ی درس‌ها را چک کن** (بدون اندروید، در یک ثانیه):

   ```
   ./tools/check_lessons.sh
   ```

   همه‌ی صفحه‌ها، صحنه‌ها، هدف‌های لمسی، کلیدهای صدا و مجموعه‌ی سؤال‌ها را بررسی می‌کند.

۲. **ورژن بزن.** فایل `VERSION` را بالا ببر (باگ → رقم سوم، قابلیت → رقم دوم).
   `versionCode` خودکار از همین فایل ساخته می‌شود.

۳. **کامپایل روی GitHub Actions است** (Android SDK در محیط توسعه نیست؛ `dl.google.com` بسته است):
   `.github/workflows/build-apk.yml` روی هر پوش به `master` اجرا می‌شود. وضعیت ران را از
   `api.github.com` چک کن.

۴. **خروجی همیشه همین‌جاست** (آدرس ثابت):
   <https://github.com/mojtaba-pourasghar/hohoapp_olom_p3e/releases/download/latest/hoohoo-olom.apk>
   بیلد ریلیز `latest` و `v<VERSION>` را می‌سازد و `dist/hoohoo-olom.apk` را کامیت می‌کند.

> پوش‌هایی که خودِ اکشن انجام می‌دهد ورک‌فلو را تریگر نمی‌کنند؛ اگر لازم شد دستی dispatch کن.
> اگر انتشار شکست، ورک‌فلو خودش یک ایشو با دلیل خطا باز می‌کند.

## درس‌ها: هوهو معلم است و تصویرهای کتاب را متحرّک می‌کند (روشِ اصلی)

**صفحه‌ی کتاب هرگز در اپ نشان داده نمی‌شود و خط به خط خوانده نمی‌شود** (خواسته‌ی صریحِ صاحبِ پروژه).
هوهو مثل معلم با توضیحِ زیاد درس می‌دهد و تصویرهای خودِ کتاب را، بریده و جدا، روی یک «صحنه»
متحرّک می‌کند.

- **بریدنِ تصویرها**: فهرست در `project/art/art.txt`، هر خط
  `name | صفحه‌ی چاپی | x1 y1 x2 y2 | flags | erase` (مختصات به هزارمِ صفحه).
  `k` = پس‌زمینه‌ی ساده‌ی لبه‌ها شفاف شود، `r` = گوشه‌ی گرد (برای عکس‌ها)، `erase` = مستطیل‌هایی
  (با `;` جدا) که قبل از بریدن سفید می‌شوند (نوشته یا خطِ حاشیه‌ای که روی تصویر افتاده).
  خروجی `app/src/main/assets/art/<name>.webp`:

  ```
  pip install pymupdf pillow numpy scipy
  python3 tools/cut_art.py                 # همه (کُند است)
  python3 tools/cut_art.py cheetah m_      # فقط اسم‌هایی که با این‌ها شروع می‌شوند
  ```

  صفحه‌ی اولِ PDF جلد است، پس صفحه‌ی چاپیِ N همان ایندکسِ N در PDF است. اسم‌ها یکتا باشند.
- **صحنه** (`StageSpec.scene(...)`، `data/Scene.java`، `ui/SceneRenderer.java`): صحنه‌ی ۱۰۰۰×۶۰۰،
  زمان‌ها کسری از طولِ صدای هوهو (۰ تا ۱). دستورها:
  `bg sky|farm|kitchen|class|lab|water|night|park|sea|desert|snow|shop|warm|plain`،
  `img NAME x y w [pop|fade|left|right|top|bottom|grow|none] [t] [bob sway spin shake pulse float flip tilt]`،
  `text "…" x y [t] [رنگ] [اندازه]` (متنِ `"?:…"` تا جواب دادن پنهان است)،
  `arrow x1 y1 x2 y2 [t] [رنگ] [curve]`، `beam`، `ring I [t]`، `move I x y t1 t2`، `scale I s t1 t2`،
  `turn I deg t1 t2`، `hide I t`، `show I t`، `rain|snow x y w h [t1 t2]`، `steam|bubbles x y [t1 t2]`،
  `rays x y r [t]`، `sparkle x y [t]`، `box x1 y1 x2 y2 [t] [رنگ]`. `I` شماره‌ی `img` است (از ۱).
  رنگ‌ها: orange teal pink blue green red yellow purple white ink. در متنِ صحنه از «←» استفاده نکن.
- انیمیشن با `Choreographer` اجرا می‌شود نه `ValueAnimator` (تنظیم «مقیاس انیمیشن» سیستم).
- **نوع گام‌ها** (`LessonKind`): `TEACH` توضیح + `.withExample(...)` («یک مثال دیگر»)، `MCQ`، `NUM`،
  `TAP` با `LessonStep.pick(key, say, caption, scene, on(n…), why)` (کودک روی تصویرِ درست می‌زند؛
  n شماره‌ی `img`)، `DONE` پایانِ قسمت. کلیدها `tPPP_NN` (مثال `…x`، بازخورد `…f`).
- **دیدنِ صحنه‌ها**: `python3 tools/preview_scenes.py /tmp/sc 't06[1-9]'` هر صحنه را در لحظه‌ی آخر
  می‌کشد؛ قبل از کامیت همه را با چشم ببین (تصویرِ بریده‌شده‌ی خراب، روی هم افتادن، بیرون‌زدگی).
- متن‌ها در `data/Chapter<N>Pages.java` (N = ۱ تا ۱۴)، یک `LessonScript` برای هر صفحه‌ی کتاب
  (در اپ «قسمت» نامیده می‌شود)؛ `data/PageLessons.java` آن را از روی `Book` پیدا می‌کند.
- **«خلاصه‌ی بخش»** (`data/Lessons.java`) جدا نوشته نمی‌شود: از سؤال‌های قسمت‌های همان بخش ساخته
  می‌شود.
- حالتِ قدیمیِ `StageSpec.book` (دوربین روی صفحه‌ی کتاب) در کد مانده ولی استفاده نمی‌شود و
  `assets/book` حذف شده؛ برنگردانش.

### لحن و شکلِ درس (مهم)

- **هوهو معلم است، نه گوینده‌ی کتاب.** «کتاب می‌گوید» و «صفحه‌ی بیست» نگو. مثل معلمِ کلاسِ سوم
  با جمله‌های ساده و زیاد توضیح بده، به زندگیِ کودک ربط بده، سؤال بپرس، مثال بزن.
- هر تصویر و فعالیتِ مهمِ صفحه باید بیاید و در صحنه همان تصویری متحرّک شود که هوهو درباره‌اش حرف می‌زند.
- ترتیب: توضیح → «یک مثال دیگر» → نوبتِ کودک (لمس روی تصویر یا جواب) → بازخورد → `DONE`.
- **جواب را لو نده.** در جوابِ غلط، `why` نشان داده نمی‌شود؛ هوهو فقط می‌گوید دوباره نگاه کن.
  والدین با «نمایش پاسخ» هدف‌های لمسی را سبز می‌بینند.

## درس‌ها و بخش‌ها

`data/Book.java` فهرست ۱۴ درس است. **تعداد بخش‌های هر درس فرق دارد** (۱ تا ۴)؛ همیشه
`chapter.sections.size()` بپرس. در کد به «درس» هنوز `chapter` گفته می‌شود.

## تمرین، کاربرگ، آزمون

- سؤال‌ها **دست‌نویس‌اند** (علوم فرمول ندارد): `project/quiz/cNN.txt`، هر خط یک سؤال:

  ```
  M|بخش|سطح|سؤال|توضیح|جواب درست|غلط۱|غلط۲|غلط۳
  T|بخش|سطح|جمله|true/false|توضیح
  ```

  بعد `python3 tools/make_quiz.py` فایل‌های `data/Chapter<N>Quiz.java` را می‌سازد (دستی ویرایش نکن).
- `QuizBank.forChapter` این بانک را با همه‌ی سؤال‌های چندگزینه‌ایِ صفحه‌های همان درس جمع می‌کند.
- `QuizBuilder`: تمرینِ بخش (سؤال‌های همان بخش، آسان به سخت)، کاربرگ (تا ۳۰ سؤال، سطحِ انتخابی
  اول)، آزمون (۱۵ سؤال، یکی‌یکی از هر بخش). هر دور ترتیب و جای گزینه‌ها عوض می‌شود.
- دکمه‌ی «راهنما» جواب را نمی‌گوید؛ بخش و صفحه‌های کتاب را نشان می‌دهد.

## صداها

**گفتارها در `res/raw/<key>.ogg` هستند** (`fa-IR-DilaraNeural`، سرعت ‎-۸٪، مثال‌ها ‎-۲۵٪؛ با
ورک‌فلوی `voice.yml` ساخته می‌شوند) و داخلِ APK می‌روند. هر جمله‌ای که فایل نداشته باشد با موتور گفتار دستگاه خوانده می‌شود.
اگر متنِ درسی عوض شد، منیفست را دوباره بساز و `voice.yml` را اجرا کن. متنِ هر ضبط در
`project/voice_spoken.json` ثبت می‌شود، پس جمله‌ی تغییرکرده خودش دوباره ضبط می‌شود و ضبطِ
کلیدهایی که از منیفست رفته‌اند پاک می‌شود (`force` همه را از نو می‌سازد).
`res/raw/audio_manifest.txt` از روی درس‌ها و سؤال‌ها ساخته می‌شود:

```
python3 tools/make_manifest.py
```

هر خط: `کلید | متن | تلفظ با اعراب`. `tts/NarrationText` ستون سوم را می‌خواند؛ هر وقت
`<key>.ogg` در `res/raw` گذاشته شود، همان پخش می‌شود. ساخت صدا روی رانر: ورک‌فلوی `voice.yml`
یا تیکِ `make_voice` در بیلدِ دستی (`fa-IR-DilaraNeural`). جدول اعراب در `tools/vowels.py`.
کلیدهای مرورِ بخش (`chNN_sN_intro/done`) پویا هستند و در منیفست نیستند.

## نکته‌های فنی که قبلاً گاز گرفته‌اند (از پروژه‌ی ریاضی)

- `<circle>` در vector drawable نیست؛ با `<path>` و کمان بکش.
- `ContextCompat.getColor(c, 0)` استثنا می‌دهد.
- **هر کنترلی که به `layout/` اضافه می‌کنی، در `layout-land/` و `layout-sw600dp/` هم بگذار**
  (کنترل‌ها با `onTap`/`setText` نال‌امن بسته می‌شوند، ولی لی‌اوت‌ها را هم‌تراز نگه دار).
- صدای صفحه‌ی تازه با `view.post(...)` در فریم بعد شروع می‌شود.
- لاگ‌های Actions از محیط توسعه بسته است؛ خطا را ورک‌فلو به‌صورت ایشو گزارش می‌کند.

## قواعد محتوا و پیمایش

- **هیچ‌چیز قفل نیست.** «کلاس تا کجا رسیده» فقط نشانک است.
- جای کودک نگه داشته می‌شود (تمرین نیمه‌کاره، گامِ درس، گامِ صفحه).
- رفت و برگشتِ آزاد در درس و آزمون.
